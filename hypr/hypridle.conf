# General configuration
# general {
# 	lock_cmd = pidof hyprlock || hyprlock  		# Prevent multiple hyprlock instances
#   before_sleep_cmd = loginctl lock-session  	# Lock session before system suspend
# 	after_sleep_cmd = hyprctl dispatch dpms on  # Re-enable display after waking from suspend
# 	ignore_dbus_inhibit = false  				# Respect DBus idle-inhibit requests (e.g., from media players)
# 	ignore_systemd_inhibit = false  			# Respect systemd idle inhibitors
# }

# Listener 1: Dim screen brightness after 4 minutes (240 seconds)
listener {
	timeout = 240
	on-timeout = brightnessctl -s set 10  	# Dim monitor backlight to minimum (avoid 0 for OLED)
	on-resume = brightnessctl -r  			# Restore previous brightness on activity
}

# Listener 2: Lock screen after 5 minutes (300 seconds) of inactivity
listener {
	timeout = 300
	on-timeout = hyprlock  						# Launch hyprlock to lock the screen
	on-resume = notify-send -h int:transient:1 "Welcome back!"  	# Optional notification on activity resumption
}

# Listener 3: Turn off screen (DPMS) after 7 minutes (420 seconds)
listener {
	timeout = 420
	on-timeout = hyprctl dispatch dpms off	# Turn off display
	on-resume = hyprctl dispatch dpms on  	# Turn on display when activity resumes
}


# listener {
#     timeout = 180
#     on-timeout = notify-send "Locking in 1 minute"
# }
#
# listener {
#     timeout = 240
#     on-timeout = hyprlock
# }
#
# listener {
#     timeout = 360
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
